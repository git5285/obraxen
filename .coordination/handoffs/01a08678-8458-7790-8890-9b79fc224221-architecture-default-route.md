# Handoff: arquitectura como portada predeterminada

- thread_id: 01a08678-8458-7790-8890-9b79fc224221-architecture-default-route
- estado: candidata lista para revisión, 2026-09-09
- alcance: la raíz redirige a `/es/` y la portada española renderiza la composición de arquitectura ya validada. `/en/`, `/de/` y `/fr/` conservan la portada heredada hasta disponer de sus equivalentes.
- decisiones: `defaultLocale` pasa a `es` para que `x-default` coincida con la raíz. La metadata española se hace específica de arquitectura y no declara equivalencias hreflang inexistentes. La preview de arquitectura y su endpoint de medios no se alteran.
- validación: Vitest 69/69 en rutas, portada y arquitectura; TypeScript sin emisión; ESLint de los archivos afectados; detector Impeccable limpio; comprobación local de `/` → `/es/` y del árbol accesible de la portada.
- límite: no hay publicación, despliegue ni activación de producción. Los contenidos de arquitectura siguen siendo una candidata local y los otros idiomas requieren traducción y revisión antes de sustituir sus portadas.
