# Handoff: Fase 1 — Fundacion Next.js

- thread_id: eee63cb7-5426-4be9-a150-02321b76e0f2
- terminado: 2026-07-14 18:46 Europe/Madrid
- resultado: completada la fundacion paralela Next.js App Router sin sustituir la web estatica; incluye TypeScript estricto, ESLint, Vitest, Node 24 fijado, datos Zod, puerta de publicacion y rutas estaticas de control.
- integracion_previa: la Fase 0 se publico mediante la PR `#1` y quedo fusionada en `main` con `2ec10f3`; esta entrega parte de ese merge en `codex/next-foundation`.
- archivos_cambiados: `package.json`, `package-lock.json`, `.nvmrc`, `.gitignore`, `tsconfig.json`, `next-env.d.ts`, `next.config.ts`, `eslint.config.mjs`, `vitest.config.ts`, `src/app/**`, `src/lib/**`, `tests/**`, `README.md`, `ROADMAP.md`, `TECHNICAL_AUDIT.md`, `COORDINATION.md`, claim y handoff propios.
- arquitectura: `npm run build` y Vercel siguen generando `dist/`; `build:next` es una via paralela hasta la paridad. La raiz Next es una pagina interna minima, usa Server Components y los tokens industriales existentes. No se añadieron Tailwind, shadcn, fuentes remotas ni codigo cliente.
- datos_y_publicacion: Zod valida identidad, seis proyectos y ofertas; comprueba slugs unicos y referencias de evidencia. `publicar: true` falla si falta identidad, sociedad, dominio, contacto o revision legal; la preview produce `noindex`, `nofollow`, `nocache` y `Disallow: /`.
- versiones: Next.js `16.2.10`, React/React DOM `19.2.7`, Zod `4.4.3`, TypeScript `6.0.3`, ESLint `9.39.5`, Vitest `4.1.10`; Node objetivo `24.x`.
- verificaciones: `npm run check:all` correcto; 3 ficheros y 10 tests correctos; Next prerenderiza `/`, `/_not-found` y `/robots.txt`; `npm audit` devuelve 0 vulnerabilidades; Chromium headless valida 390x844 y 1440x1000, un `h1`, sin overflow ni errores de consola, cabeceras CSP/nosniff, ausencia de `x-powered-by`, robots cerrado y 404 correcto; `git diff --check` correcto.
- entorno: las verificaciones locales se ejecutaron con Node `26.5.0`; `.nvmrc` y `engines` fijan `24.x`, version LTS y disponible en Vercel. La Fase 4 debe ejecutar la matriz reproducible en Node 24 mediante GitHub Actions.
- seguridad_dependencias: `postcss` queda forzado a `8.5.19` para evitar la version vulnerable transitiva; el arbol `npm ls` es coherente y la auditoria no reporta vulnerabilidades.
- pendiente: Fase 2 debe migrar portada y menu con paridad visual y de rendimiento antes de cambiar la salida primaria; Fases 3–6 conservan proyectos, soluciones, CI, consentimiento y publicacion. Naming, sociedad, dominio, contacto y legal siguen sin inventar y sin autorizar despliegue.
- commit_push_deploy: commit local de Fase 1 realizado al cierre de la entrega; no se ha hecho push, PR ni despliegue de Fase 1.
- estado: claim liberada; lista para revision e integracion secuencial.
