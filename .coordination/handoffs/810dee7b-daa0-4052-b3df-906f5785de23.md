# Handoff: memoria duradera y autonomía local v2

- thread_id: 810dee7b-daa0-4052-b3df-906f5785de23
- terminado: 2026-07-16 17:19 Europe/Madrid
- rama: `codex/autonomous-agents`
- base_sha: `218655fcde62b8e3582e56497593de862d46defe`
- resultado: el ciclo de seis horas dispone de memoria persistente, contexto
  acotado, contrato final estricto, deduplicación, telemetría no inventada y
  límite determinista de un solo diff local pendiente. Tras autorización
  expresa del usuario y tres canarios sombra se promueve únicamente a
  `active + allowLocalDiff`.

## Entrega

- `memory.mjs` guarda episodios inmutables y estado derivado bajo el directorio
  Git común, compartido por todos los worktrees y fuera de los commits.
- El contexto incluye solo ejecuciones recientes y hallazgos relevantes; un SHA
  distinto exige revalidación. Las reglas aprendidas permanecen en cuarentena y
  su texto no entra como instrucción.
- `validateRunReport` exige el conjunto exacto de campos, coherencia entre modo,
  estado y mutación, y telemetría medida o `null`.
- `preflight` bloquea un segundo writer cuando una claim está
  `esperando_revision`, aunque el scout puede seguir inspeccionando.
- Skill, scout, evals y automatización local consumen memoria antes de decidir y
  registran el informe exacto después del ciclo.
- `AUTONOMOUS_IMPROVEMENT.md` explica arquitectura, memoria, estados,
  disponibilidad, promoción y recuperación.
- El workflow `.github/workflows/remainon-continuous-improvement.md` compila en
  estricto con motor Codex, memoria de repositorio, permisos de lectura y salida
  `noop`; permanece manual y en sombra.

## Canarios

- tres scouts reales y paralelos, mismo preflight y SHA `218655f`;
- conservador: `no_op`;
- solo-medible y contrario: confirmaron independientemente el mismo hueco de
  bypass por teclado en contacto, fuera de la claim del sistema;
- los tres contratos scout validaron y ningún agente creó archivos o mutó Git;
- memoria: 3 episodios, 1 `no_op`, 2 `shadow_finding`, un hallazgo deduplicado
  con 2 ocurrencias, 0 reglas propuestas y telemetría sin estimar.

## Verificaciones

- `npx vitest run tests/agents/memory.test.ts tests/agents/preflight.test.ts tests/agents/policy.test.ts`: 40/40;
- `npm run check:quality`: 153 unitarias, 2 pruebas de contacto habilitado,
  88 Playwright correctas, 7 omisiones previstas, WebKit correcto y Lighthouse
  dentro de presupuesto en tres rutas;
- `npm run check:diff -- --base-sha 218655fc...`: limpio tras normalizar
  whitespace del lock generado;
- `npm run check:activation -- --json`: salida válida, exit 1 esperado,
  `NO-GO`, 35 blockers, publicación y publish switch en falso;
- `gh aw validate`: workflow válido; compilación estricta y Poutine sin avisos;
- `gh aw lint` no pudo invocar actionlint porque Docker no está instalado; es
  una limitación local de herramienta, no un error de esquema del workflow.

## Autoridad y límites

- permitido: scout, memoria y un único diff local aislado;
- deshabilitado: dependencias, red del builder, commit, push, draft PR, merge,
  deploy y publish;
- el workflow alojado no tiene horario ni credencial todavía. Para 24/7 requiere
  `OPENAI_API_KEY` o `CODEX_API_KEY` y un presupuesto explícito;
- la automatización local queda pausada durante el commit/cierre de esta claim y
  se reanuda después para evitar colisión con la entrega.

## Pendiente controlado

1. Ejecutar el primer canario activo sobre el hallazgo de skip link en un
   worktree, claim y lease aislados; conservarlo como diff local para revisión.
2. No habilitar commit/PR alojado hasta revisar ese canario y aportar credencial
   y presupuesto.
3. Nunca automatizar merge, despliegue o publicación.

- estado: claim lista para liberar tras commit exacto; sin despliegue ni
  publicación.
