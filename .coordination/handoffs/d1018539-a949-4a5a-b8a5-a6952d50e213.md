# Handoff: reparación fail-closed del preflight y promoción de learned_rules
- thread_id: d1018539-a949-4a5a-b8a5-a6952d50e213
- terminado: 2026-07-16 15:30 Europe/Madrid
- resultado: `DISCOVERED-20260716-01` reparado con regresiones unitarias y causa
  raíz local corregida; `INTAKE-20260716-01` promovido a la skill productiva;
  gate de calidad completo verde; sin push, PR, despliegue ni publicación.
- rama: `codex/autonomous-agents`
- archivos_cambiados:
  - `automation/agents/preflight.mjs`: nueva función pura `deriveGating`;
    `eligibility.scout` y `eligibility.writer` exigen escaneo completo;
    `unreadable_worktrees` como blocker; `unreadableWorktrees` y
    `claimFailures` expuestos como evidencia; lectura de claims por worktree
    protegida (un fallo de lectura equivale a worktree ilegible).
  - `automation/agents/preflight.d.mts`: tipos `GatingInput`/`Gating` y firma
    de `deriveGating`.
  - `tests/agents/preflight.test.ts`: cuatro regresiones, incluida la
    reproducción exacta del fixture `preflight-unreadable.json` de eval-10.
  - `.agents/skills/remainon-continuous-improvement/SKILL.md`: campo
    `learned_rules` en el informe final + sección «Learned rules» (solo
    `proposed`, con procedencia, sin autoedición; promoción siempre humana).
  - `.agents/skills/.../evals/intake-20260716/decisions.md`: sección de
    seguimiento con los nuevos estados (`promoted`, `repaired`).
  - `COORDINATION.md`: tablero actualizado con las tres entregas del sistema de
    agentes.
  - claim y handoff propios.
- verificaciones:
  - `npx vitest run tests/agents/`: 23/23 en verde;
  - `QA_PORT=3219 npm run check:quality`: exit 0 — lint, tipos, 126 unitarias
    (14 archivos), build de 52 páginas, 88 Playwright correctas con 7 omisiones
    intencionales y Lighthouse dentro de presupuesto en las 3 rutas;
  - `git diff --check`: limpio;
  - `node automation/agents/preflight.mjs --json` en vivo: worktrees legibles,
    `claimFailures: []`, scout elegible solo con `policy_mode_shadow` como
    blocker, y la claim de esta tarea detectada como activa hasta su
    liberación;
  - todos los JSON del intake validados; sin archivos > 1 MB.
- decisiones:
  - la causa raíz local era `core.bare=true` escrito por accidente en
    `.git/config` del repositorio principal el 2026-07-16 01:28 (coincide con el
    cierre del hilo que creó el sistema); sin mención en código, hooks ni
    documentación, se restauró a `false` y el worktree principal quedó legible
    y limpio. La reparación de código se mantiene igualmente: el preflight debe
    fallar cerrado ante cualquier ilegibilidad futura, sea cual sea su causa;
  - procedencia separada en commits: los artefactos del intake `71dfbceb...` se
    registraron tal como los dejó esa tarea, y esta intervención va en commits
    propios;
  - los ítems 03 y 05 permanecen `pending` por diseño; no se ejecutaron
    canarios ni se cambió ningún modelo;
  - merge, push, PR, despliegue y publicación siguen siendo decisiones humanas;
    esta entrega no realizó ninguna acción externa.
- pendiente:
  - decisión humana de cadencia del ciclo sombra (recomendación previa: cada
    6 horas) y ejecución de los tres canarios sombra revisados;
  - umbral humano de coste y telemetría antes de evaluar los ítems 03 y 05;
  - push de la rama y revisión de la PR cuando el usuario lo decida.
- mensaje_enviado_a: usuario de esta tarea; chat «Crear agentes» como
  consumidor del registro del intake.
