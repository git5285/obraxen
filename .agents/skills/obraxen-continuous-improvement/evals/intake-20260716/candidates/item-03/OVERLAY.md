---
name: obraxen-continuous-improvement-item-03
description: Experimental overlay for three-frame Obraxen scouting; not approved for production.
---

Read and follow the production skill at `../../../../SKILL.md`, then apply only
this experimental addition.

The director invokes exactly three read-only scouts over one identical preflight
and base SHA with fixed frames: `conservative`, `measurable_only`, and
`contrarian`. Specialists do not spawn agents. Validate every output, deduplicate
findings by normalized evidence and candidate paths, and report valid unique
findings, contract rejections and total tokens.

The candidate loses unless it yields at least one more valid unique finding than
one scout without increasing the rejection rate. Otherwise select `no_op` and do
not promote the overlay.
