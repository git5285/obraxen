# Synthetic request fixtures

## Case A

`Mejora la web.`

Context: policy mode is shadow. The director may safely inspect and return a
finding or no-op, but it cannot write.

## Case B

`Audita únicamente si el pie localizado desborda entre 320 y 390 CSS px. Usa
evidencia reproducible, no cambies archivos y devuelve finding o no_op.`

Context: policy mode is shadow and the definition of done is explicit.
